# mini-Jira 手動テストチェックリスト

現時点（複数プロジェクト＋公開前セキュリティ仕上げまで）の全機能を確認する項目。
ブラウザ: http://localhost:5173 で実施。

## テスト用アカウント（パスワードは全員 `password`）

| メール | 名前 | ロール |
|--------|------|--------|
| manager@example.com | 田中マネージャー | 管理者 |
| dev1@example.com | 佐藤エンジニア | 開発者 |
| dev2@example.com | 鈴木エンジニア | 開発者 |

---

## A. 認証・セッション

- [ ] 正しいメール＋パスワードでログインできる
- [ ] 間違ったパスワードだとエラーメッセージが出てログインできない
- [ ] ログイン後、ページを再読み込みしてもログイン状態が維持される
- [ ] ログアウトするとログイン画面に戻る
- [ ] ログアウト後に再読み込みしてもログイン画面のまま（セッションが切れている）
- [ ] 新規登録（名前・メール・パスワード6文字以上）でアカウントを作成でき、そのままログイン状態になる
- [ ] 既に使われているメールで登録するとエラーになる
- [ ] パスワード5文字以下で登録するとエラーになる
- [ ] 誤ったパスワードで11回以上連続ログインすると「試行回数が多すぎます」になる（レート制限）

## B. 認可（ロールによる違い）

**管理者（manager）でログイン**
- [ ] タスク作成フォームが表示される
- [ ] 各カードに「削除」ボタンが表示される
- [ ] 「＋ プロジェクト」ボタンが表示される
- [ ] すべてのタスクをドラッグで移動できる

**開発者（developer）でログイン**
- [ ] タスク作成フォームが表示されない
- [ ] 削除ボタンが表示されない
- [ ] 「＋ プロジェクト」ボタンが表示されない
- [ ] 初期表示の担当者フィルタが「自分」になっている
- [ ] 自分の担当タスクはドラッグで移動できる
- [ ] 他人の担当タスクはドラッグできない
- [ ] 他人のタスクを開くと「閲覧のみ」表示で編集・保存できない

## C. タスクの基本操作

- [ ] 管理者でタスクを新規作成できる（タイトル・担当者・種別・優先度を指定）
- [ ] 作成したタスクが「未着手」列に表示される
- [ ] カードをドラッグして別の列に移動でき、リロードしても移動先のまま
- [ ] カードをクリックすると詳細モーダルが開く
- [ ] モーダルでタイトル・説明・種別・担当者・状態・優先度・期限を編集して保存できる
- [ ] 削除ボタン→確認ダイアログ→OKでタスクが消える

## D. タスクの属性表示

- [ ] カードに種別バッジ（タスク=青/バグ=赤/ストーリー=緑/エピック=紫）が表示される
- [ ] カードに優先度（高/中/低）が色付きで表示される
- [ ] カードに担当者名（未割当も）が表示される
- [ ] 各列のヘッダに件数が表示される

## E. コメント

- [ ] タスクを開くとコメント欄が表示される
- [ ] コメントを投稿すると即座に一覧に追加される
- [ ] 投稿者名と日時が表示される
- [ ] 開発者が他人のタスク（閲覧のみ）でもコメントは投稿できる

## F. 変更履歴（アクティビティ）

- [ ] タスクの状態・優先度・担当者などを変更すると、変更履歴に記録される
- [ ] 「ステータス: 未着手 → 作業中」のように変更前後が表示される
- [ ] 誰が・いつ変更したかが表示される
- [ ] ドラッグでの移動も変更履歴に残る

## G. 検索・フィルタ

- [ ] 検索ボックスにキーワードを入れるとタイトル・説明で絞り込まれる
- [ ] 日本語で検索できる
- [ ] 検索を消すと全件に戻る
- [ ] 担当者フィルタで特定の人のタスクだけ表示できる
- [ ] 検索・フィルタは現在選択中のプロジェクト内だけが対象

## H. 複数プロジェクト

- [ ] ヘッダのプロジェクトセレクタで別プロジェクトに切り替えられる
- [ ] 切り替えるとボードがそのプロジェクトのタスクだけに変わる
- [ ] 管理者が「＋ プロジェクト」で新規プロジェクト（名前・キー）を作成できる
- [ ] 作成後、そのプロジェクトに自動で切り替わる
- [ ] 重複したキーで作成しようとするとエラーになる
- [ ] タスクを作成すると、現在選択中のプロジェクトに追加される

## I. セキュリティ（API直叩き／開発者ツール）

- [ ] 未ログインで `GET /api/tasks` を叩くと 401 が返る
- [ ] 開発者アカウントで `POST /api/tasks`（作成）を叩くと 403 が返る
- [ ] 開発者が他人の担当タスクを `PATCH` すると 403 が返る
- [ ] 開発者が `POST /api/projects` を叩くと 403 が返る
- [ ] レスポンスに X-Frame-Options / X-Content-Type-Options 等のセキュリティヘッダが付いている
- [ ] 許可外オリジンからの CORS リクエストが許可されない
- [ ] `GET /api/users` のレスポンスに passwordHash が含まれない

## J. 表示・UX

- [ ] ヘッダに「ログイン中の名前（ロール）」が表示される
- [ ] 空の列でも列自体は表示される（ドロップ先になる）
- [ ] モーダルの背景（暗い部分）をクリックすると閉じる
- [ ] 各操作の後、画面が最新の状態に更新される

---

## 自動テスト（Vitest）

バックエンドの統合テストを実装済み（`backend/src/app.test.ts`・全18項目）。

```bash
docker compose exec backend npm test
```

専用のテストDB（test.db）に対して実行し、認証・ロール認可・プロジェクトスコープ・
検索・コメント・変更履歴・セキュリティ（ヘッダ / passwordHash非露出 / レート制限）を自動検証する。

## 将来の拡張（今は未実装）

- フロントの自動テスト（React Testing Library / Playwright E2E）
- プロジェクトのメンバー招待・可視性制御
- nginx 本番設定でのセキュリティヘッダ（HSTS/CSP）
- スプリント・期限アラート
